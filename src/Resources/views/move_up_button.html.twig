{% if not search %}
<a class="btn btn-sm btn-outline-secondary" href="{{ generate_admin_url(admin.entityClass, 'move_up', {'id': object.id}) }}">
    <i class="fa fa-arrow-circle-up"></i>
</a>
{% endif %}
